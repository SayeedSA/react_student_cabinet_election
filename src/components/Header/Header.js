import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <div className="header-container">
            <h2 className="header-title">Student Cabinet Election</h2>
            <p>Student cabinet election is a process to select candidates for election</p>
            <h3>Total Yearly Income of each candidate : 50000</h3>
        </div>
    );
};

export default Header;